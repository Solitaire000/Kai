# 只在要跑 scripts/train_lora.py 的GPU机器上安装：
#   pip install -r requirements-train.txt
# 日常部署（对话/Skills/记忆）不需要这些，故意不写进 requirements.txt，
# 避免在轻量设备/手机(Termux)上安装时被这些大包拖垮。
# 训练不依赖于venv，根据具体情况使用python版本

torch>=2.2.0            # 按你的CUDA版本去 pytorch.org 选对应安装命令，这里给的是通用最低版本
transformers>=4.44.0
peft>=0.12.0
bitsandbytes>=0.43.0    # QLoRA 4bit量化训练依赖，Windows下如遇到装不上，优先用WSL2跑训练脚本
accelerate>=0.33.0
datasets>=2.20.0
huggingface_hub>=0.24.0

openai>=1.40.0
pyyaml>=6.0
requests>=2.31.0
numpy>=1.24.0