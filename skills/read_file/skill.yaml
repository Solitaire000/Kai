name: read_file
description: "读取工作区内某个文本文件的内容（只读，不会修改文件）。"
category: filesystem
dangerous: false
parameters:
  type: object
  properties:
    path:
      type: string
      description: "相对于工作区根目录的文件路径，比如 notes/todo.txt"
  required: ["path"]
preview_template: "读取文件: {path}"
